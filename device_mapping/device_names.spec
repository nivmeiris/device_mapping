# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['device_names.py'],
    pathex=[],
    binaries=[],
    datas=[('mapping_devices.txt', '.'), ('device_names.txt', '.'), ('mapping_ios_devices.txt', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='device_names',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
app = BUNDLE(
    exe,
    name='device_names.app',
    icon=None,
    bundle_identifier=None,
)
